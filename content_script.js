// In Next release - add changed url feature, via sendMeesage / check storage

// window.addEventListener("hashchange", function (event) {
//     alert("old url: " + event.oldURL.toString() + ", new url: " + event.newURL.toString());
// });
